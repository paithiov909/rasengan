% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field.R
\name{as_field}
\alias{as_field}
\title{Coerce an object to a field function}
\usage{
as_field(field)
}
\arguments{
\item{field}{An object representing a field. Supported inputs include:
\itemize{
\item A function with signature \verb{function(seed, n_steps, step_size, params)}, returning a trajectory.
\item A data frame with columns \code{x}, \code{y}, \code{vx}, and \code{vy}, interpreted as a discrete vector field.
}}
}
\value{
A function of class \code{"field_fn"} that generates trajectories
from seed points.
}
\description{
\code{as_field()} converts various representations of a vector field or
field-like object into a \emph{field function} (a function of class
\code{"field_fn"}). A field function takes a seed point and tracing
parameters, and returns a trajectory as a data frame.

The returned function must have the signature:
\verb{function(seed, n_steps, step_size, params)}, where \code{seed} is a data frame
with columns \code{x} and \code{y}, and the return value is a data frame with
columns \code{step}, \code{x}, and \code{y}.
}
\details{
For data frame inputs, \code{as_field()} constructs a simple trajectory
generator that iteratively advances a point by looking up the nearest
vector \verb{(vx, vy)} and applying a fixed step size.

More advanced behaviors (e.g., vectorized evaluation, custom integration
schemes, or compiled implementations) can be achieved by supplying a
user-defined function directly.
}
\examples{
# Using a custom field function
f <- function(seed, n_steps, step_size, params) {
  x <- numeric(n_steps)
  y <- numeric(n_steps)
  cur <- as.matrix(seed[, c("x", "y"), drop = FALSE])
  for (i in seq_len(n_steps)) {
    x[i] <- cur[, 1]
    y[i] <- cur[, 2]
    cur <- cur + step_size * c(1, 0)
  }
  data.frame(step = seq_len(n_steps), x = x, y = y)
}

as_field(f)

# Using a data frame as a vector field
df <- data.frame(
  x = runif(10),
  y = runif(10),
  vx = runif(10, -1, 1),
  vy = runif(10, -1, 1)
)

as_field(df)
}
\seealso{
\code{\link[=trace_field]{trace_field()}}
}
