% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve-bezier.R
\name{bezier}
\alias{bezier}
\alias{curve_bezier}
\alias{bezier_derivative}
\alias{bezier_normal}
\alias{bezier_tangent}
\title{Bezier curve utilities}
\usage{
curve_bezier(n, control_pts)

bezier_derivative(n, control_pts)

bezier_normal(n, control_pts)

bezier_tangent(n, control_pts)
}
\arguments{
\item{n}{An integer scalar; Number of points to sample along the curve.}

\item{control_pts}{A numeric matrix or data frame of control points.}
}
\value{
A tibble.
}
\description{
Functions for evaluating cubic Bezier curves and their associated vector
quantities. A cubic Bezier curve is defined by four control points
\eqn{P_0, P_1, P_2, P_3}, where \eqn{P_0} and \eqn{P_3} are endpoints and
\eqn{P_1}, \eqn{P_2} act as handles controlling the tangent directions.

All functions accept a 8 column numeric matrix or data frame
containing control points, where each row encodes the four points as:
\verb{(x0, y0, x1, y1, x2, y2, x3, y3)}.
}
\details{
\itemize{
\item \code{curve_bezier(n, control_pts)}:
Evaluate cubic Bezier curves at \code{n} evenly spaced parameter values in
\eqn{t \in [0, 1]}.
Returns a table of \verb{(x, y)} positions for each curve \code{id}.
\item \code{bezier_derivative(n, control_pts)}:
Compute first derivatives of each curve at \code{n} points.
The result contains the velocity vectors \verb{(dx, dy)} at each \code{t}.
\item \code{bezier_normal(n, control_pts)}:
Compute unit normal vectors \verb{(nx, ny)} along each curve.
Normals are derived from the first derivative and represent the
direction perpendicular to the tangent at each point.
\item \code{bezier_tangent(n, control_pts)}:
Compute unit tangent vectors \verb{(tx, ty)} along each curve.
Tangents represent the direction of movement along the curve and are
obtained by normalizing the first derivative.
}

These functions are useful for geometric processing, constructing smooth
interpolations, and generating direction fields for animation or
stroke-based visual effects.
}
\seealso{
\code{\link[=compute_handles]{compute_handles()}}

Other curve: 
\code{\link{curve_archimedean}()},
\code{\link{curve_involute}()},
\code{\link{curve_spring}()},
\code{\link{curve_twist}()}
}
\concept{curve}
