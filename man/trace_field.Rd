% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/field.R
\name{trace_field}
\alias{trace_field}
\title{Trace trajectories through a field}
\usage{
trace_field(seeds, field, x = x, y = y, n_steps = 10, step_size = 1, ...)
}
\arguments{
\item{seeds}{A data frame containing seed points.}

\item{field}{A field object. This can be:
\itemize{
\item A function with signature \verb{function(seed, n_steps, step_size, params)}, or
\item Any object supported by \code{\link[=as_field]{as_field()}}.
}}

\item{x, y}{<\code{\link[rlang:args_data_masking]{data-masking}}> Expressions
specifying the x and y coordinates of the seeds.}

\item{n_steps}{Integer scalar giving the number of steps in each
trajectory.}

\item{step_size}{Numeric scalar controlling the step size used by the
field function.}

\item{...}{Additional arguments passed to the field function
as a named list \code{params}.}
}
\value{
A tibble with columns:
\itemize{
\item \code{seed}: integer identifier for each input seed
\item \code{step}: step index within each trajectory
\item \code{x}, \code{y}: coordinates of the trajectory
}
}
\description{
Generates trajectories from a set of seed points using
a field function. Each seed is passed to the field function, which is
responsible for producing a trajectory of length \code{n_steps}.
}
\details{
\code{trace_field()} standardizes the input \code{field} via \code{\link[=as_field]{as_field()}} and
applies it to each seed independently. The field function is expected
to return a data frame describing the trajectory for a single seed.

The design allows flexible implementations of field behavior, including
vectorized or compiled trajectory generators.
}
\examples{
seeds <- data.frame(
  x = runif(5),
  y = runif(5)
)

field <- function(seed, n_steps, step_size, params) {
  x <- numeric(n_steps)
  y <- numeric(n_steps)
  cur <- as.matrix(seed[, c("x", "y"), drop = FALSE])
  for (i in seq_len(n_steps)) {
    x[i] <- cur[, 1]
    y[i] <- cur[, 2]
    cur <- cur + step_size * c(0.1, 0.1)
  }
  data.frame(step = seq_len(n_steps), x = x, y = y)
}

trace_field(seeds, field, x = x, y = y, n_steps = 10, step_size = 0.1)
}
