% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state-orbits.R
\name{state_orbits}
\alias{state_orbits}
\title{Create a state for orbital motion}
\usage{
state_orbits(
  seeds,
  origin = c(0, 0),
  x = x,
  y = y,
  omega = 1,
  id = dplyr::row_number()
)
}
\arguments{
\item{seeds}{A data frame containing the initial point positions.}

\item{origin}{Numeric vector of length 2 giving the center of rotation.}

\item{x, y}{<\code{\link[rlang:args_data_masking]{data-masking}}> Coordinates used as
the initial point positions.}

\item{omega}{<\code{\link[rlang:args_data_masking]{data-masking}}>
Angular velocity. May be either a scalar or a column evaluated
in \code{seeds}.}

\item{id}{<\code{\link[rlang:args_data_masking]{data-masking}}>
Identifier for each orbit. Defaults to row numbers.}
}
\value{
A list.
}
\description{
Creates a pseudo-chakra state in which points revolve around a common
origin with constant angular velocity. Initial phases and orbital radii are
inferred from the input coordinates.
}
\details{
The initial positions are interpreted as points on circular orbits around
\code{origin}. For each point, the orbital radius and initial phase are computed
internally from the supplied coordinates.

The resulting state stores:
\itemize{
\item orbital radius
\item initial phase
\item angular velocity
\item current time
}

Coordinates at the current time can be retrieved with \code{\link[=observe]{observe()}}.
}
\examples{
seeds <-
  curve_epicycloid(16) |>
  dplyr::mutate(
    omega = seq(-1, 1, length.out = dplyr::n())
  )

state <-
  state_orbits(
    seeds,
    x = x,
    y = y,
    omega = omega
  )

observe(state)

state |>
  circulate(dt = pi / 4) |>
  observe()
}
\seealso{
chakra

Other state:
\code{\link[=state_bouncing_pts]{state_bouncing_pts()}}
}
\concept{state}
