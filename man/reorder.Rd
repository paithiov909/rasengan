% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reorder.R
\name{reorder}
\alias{reorder}
\alias{rings_index}
\alias{rings}
\alias{rings.default}
\alias{rings.data.frame}
\alias{shift}
\alias{shift.default}
\alias{shift.data.frame}
\alias{snake_index}
\alias{snake}
\alias{snake.default}
\alias{snake.data.frame}
\alias{spiral_index}
\alias{spiral}
\alias{spiral.default}
\alias{spiral.data.frame}
\alias{stride_index}
\alias{stride}
\alias{stride.default}
\alias{stride.data.frame}
\alias{zigzag_index}
\alias{zigzag}
\alias{zigzag.default}
\alias{zigzag.data.frame}
\title{Reorder objects by cyclic shifts or scanning patterns}
\usage{
rings_index(nrow, ncol, byrow = FALSE)

rings(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{rings}{default}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{rings}{data.frame}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

shift(x, k)

\method{shift}{default}(x, k)

\method{shift}{data.frame}(x, k)

snake_index(nrow, ncol, byrow = FALSE)

snake(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{snake}{default}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{snake}{data.frame}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

spiral_index(nrow, ncol, byrow = FALSE)

spiral(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{spiral}{default}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{spiral}{data.frame}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

stride_index(n, step = 2L)

stride(x, step = 2L)

\method{stride}{default}(x, step = 2L)

\method{stride}{data.frame}(x, step = 2L)

zigzag_index(nrow, ncol, byrow = FALSE)

zigzag(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{zigzag}{default}(x, nrow = NULL, ncol = NULL, byrow = FALSE)

\method{zigzag}{data.frame}(x, nrow = NULL, ncol = NULL, byrow = FALSE)
}
\arguments{
\item{nrow, ncol}{Integers giving the conceptual number of rows and columns
for grid-based scanning patterns.
If one of them is \code{NULL}, it is inferred from the length of \code{x}.}

\item{byrow}{Logical; whether to interpret the implicit grid in row-major
(\code{TRUE}) or column-major (\code{FALSE}) order.}

\item{x}{A vector or data frame to be reordered.}

\item{k}{An integer shift amount for \code{shift()}.}

\item{n}{An integer scalar; the length of the index vector to generate.}

\item{step}{Integer stride size for \code{stride()} and \code{stride_index()}.}
}
\value{
A reordered object of the same type as the input (\code{x}), or an integer vector
of indices for functions ending in \verb{_index}.
}
\description{
A collection of functions for reordering one-dimensional vectors or the rows
of data frames by simple index manipulations.

These operations are designed for creative coding workflows,
where controlling the traversal order of grids or sequences is useful
for producing structured, semi-regular visual variations.
}
\details{
The following methods are available:
\itemize{
\item \code{rings(x, nrow, ncol)}:
Reorders \code{x} or the rows of a data frame by scanning the grid in
\emph{concentric rings}: the outer border first, followed by progressively
inner borders, until the center is reached.
\item \code{rings_index(nrow, ncol)}:
Returns the index vector that enumerates the cells of a grid in
concentric rectangular rings from the outside inward.
\item \code{shift(x, k)}:
Performs a circular shift of the vector \code{x} by \code{k} positions.
For data frames, rows are shifted instead of elements.
\item \code{snake(x, nrow, ncol)}:
Reorders \code{x} (or rows of a data frame) according to a \emph{snake} (serpentine)
traversal of a conceptual grid of size \code{nrow * ncol}.
Odd-numbered rows are read left-to-right, even rows right-to-left.
\item \code{snake_index(nrow, ncol)}:
Returns only the index vector representing the snake traversal.
\item \code{spiral(x, nrow, ncol)}:
Reorders \code{x} or a data frame by following a spiral scan of a grid,
starting from the outermost ring toward the center.
\item \code{spiral_index(nrow, ncol)}:
Computes the spiral traversal indices.
\item \code{stride(x, step)}:
Reorders \code{x} by interleaving elements with a stride of length \code{step}.
\item \code{stride_index(n, step)}:
Returns the index vector used by \code{stride()}.
\item \code{zigzag(x, nrow, ncol)}:
Reorders \code{x} (or rows of a data frame) according to a diagonal zigzag
traversal of a conceptual \code{nrow * ncol} grid.
Each diagonal where \code{i + j} is constant is processed together, with the
direction alternating between forward and reverse.
\item \code{zigzag_index(nrow, ncol)}:
Returns the index vector for a zigzag traversal of a grid, following
alternating diagonal scanlines (similar to JPEG DCT block ordering).
}

These functions are lightweight and composable, and are intended for
preprocessing sequences or point grids in creative visualization workflows.
}
